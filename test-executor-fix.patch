```diff
--- a/test/Telos/Tool/BashSpec.hs
+++ b/test/Telos/Tool/BashSpec.hs
@@ -10,13 +10,17 @@ import           Relude
 
 import           Telos.Tool.Bash  ( bashTool )
 import           Telos.Tool.Types ( BuiltinTool(..)
+                                  , ToolExecutorType(..)
                                   , ToolResult(..)
                                   , newToolContext
                                   , trOutput
                                   , trSuccess
                                   )
 
 import           Test.Hspec
+
+runExecutor :: BuiltinTool -> ToolContext -> Value -> IO ToolResult
+runExecutor bt = case _btExecutor bt of { SimpleExecutor f -> f; _ -> error "Not a SimpleExecutor" }
 
 spec :: Spec
 spec = do
```

```diff
--- a/test/Telos/Tool/ReadSpec.hs
+++ b/test/Telos/Tool/ReadSpec.hs
@@ -14,6 +14,7 @@ import           Relude
 
 import           Telos.Tool.Read  ( readTool )
 import           Telos.Tool.Types ( BuiltinTool(..)
+                                  , ToolExecutorType(..)
                                   , ToolResult(..)
                                   , newToolContext
                                   , trOutput
@@ -21,6 +22,9 @@ import           Telos.Tool.Types ( BuiltinTool(..)
                                   )
 
 import           Test.Hspec
+
+runExecutor :: BuiltinTool -> ToolContext -> Value -> IO ToolResult
+runExecutor bt = case _btExecutor bt of { SimpleExecutor f -> f; _ -> error "Not a SimpleExecutor" }
 
 spec :: Spec
 spec = do
```

```diff
--- a/test/Telos/Tool/WriteSpec.hs
+++ b/test/Telos/Tool/WriteSpec.hs
@@ -16,6 +16,7 @@ import           Relude
 import           Telos.Tool.Read  ( readTool )
 import           Telos.Tool.Write ( writeTool )
 import           Telos.Tool.Types ( BuiltinTool(..)
+                                  , ToolExecutorType(..)
                                   , ToolResult(..)
                                   , newToolContext
                                   , trOutput
@@ -23,6 +24,9 @@ import           Telos.Tool.Types ( BuiltinTool(..)
                                   )
 
 import           Test.Hspec
+
+runExecutor :: BuiltinTool -> ToolContext -> Value -> IO ToolResult
+runExecutor bt = case _btExecutor bt of { SimpleExecutor f -> f; _ -> error "Not a SimpleExecutor" }
 
 spec :: Spec
 spec = do
```

```diff
--- a/test/Telos/Tool/EditSpec.hs
+++ b/test/Telos/Tool/EditSpec.hs
@@ -14,6 +14,7 @@ import           Relude
 
 import           Telos.Tool.Edit  ( editTool )
 import           Telos.Tool.Types ( BuiltinTool(..)
+                                  , ToolExecutorType(..)
                                   , ToolResult(..)
                                   , ToolContext
                                   , markFileRead
@@ -23,6 +24,9 @@ import           Telos.Tool.Types ( BuiltinTool(..)
                                   )
 
 import           Test.Hspec
+
+runExecutor :: BuiltinTool -> ToolContext -> Value -> IO ToolResult
+runExecutor bt = case _btExecutor bt of { SimpleExecutor f -> f; _ -> error "Not a SimpleExecutor" }
 
 spec :: Spec
 spec = do
```

```diff
--- a/test/Telos/Tool/GlobSpec.hs
+++ b/test/Telos/Tool/GlobSpec.hs
@@ -14,6 +14,7 @@ import           Relude
 
 import           Telos.Tool.Glob  ( globTool )
 import           Telos.Tool.Types ( BuiltinTool(..)
+                                  , ToolExecutorType(..)
                                   , ToolResult(..)
                                   , newToolContext
                                   , trOutput
@@ -21,6 +22,9 @@ import           Telos.Tool.Types ( BuiltinTool(..)
                                   )
 
 import           Test.Hspec
+
+runExecutor :: BuiltinTool -> ToolContext -> Value -> IO ToolResult
+runExecutor bt = case _btExecutor bt of { SimpleExecutor f -> f; _ -> error "Not a SimpleExecutor" }
 
 spec :: Spec
 spec = do
```

```diff
--- a/test/Telos/Tool/GrepSpec.hs
+++ b/test/Telos/Tool/GrepSpec.hs
@@ -14,6 +14,7 @@ import           Relude
 
 import           Telos.Tool.Grep  ( grepTool )
 import           Telos.Tool.Types ( BuiltinTool(..)
+                                  , ToolExecutorType(..)
                                   , ToolResult(..)
                                   , newToolContext
                                   , trOutput
@@ -21,6 +22,9 @@ import           Telos.Tool.Types ( BuiltinTool(..)
                                   )
 
 import           Test.Hspec
+
+runExecutor :: BuiltinTool -> ToolContext -> Value -> IO ToolResult
+runExecutor bt = case _btExecutor bt of { SimpleExecutor f -> f; _ -> error "Not a SimpleExecutor" }
 
 spec :: Spec
 spec = do
```
